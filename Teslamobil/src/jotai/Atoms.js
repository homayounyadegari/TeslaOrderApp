import { atom } from "jotai";

//comparison sayfası için
export const Productlist = atom([]);

export const Productsdropdownlist = atom([]);

export const Oneproductlist1 = atom([]);

export const Oneproductlist2 = atom([]);

//order sayfası için

export const ProductsdropdownlistOrder = atom([]);

export const ExteriordropdownlistOrder = atom([]);

export const InteriordropdownlistOrder = atom([]);

export const WheeldropdownlistOrder = atom([]);
//orderlist sayfası için

export const Orderlist = atom([]);

export const filterAtom = atom("");